# Token do Data Hub (terceiro relatorio - HSM CX Pos-Instalacao). Segredo,
# nao commitar com valor real.
#
# Node nao carrega .env automaticamente - defina a variavel no ambiente
# antes de rodar (`export DATA_HUB_TOKEN=...` / PowerShell `$env:DATA_HUB_TOKEN=...`),
# ou rode com `node --env-file=.env src/index.js ...` (Node 20+), ou configure-a
# no processo que sobe o BFF (que e quem spawna este script via ProcessBuilder).
DATA_HUB_TOKEN=

# Credenciais de um usuario da Matrix, usadas pelo modo "api" dos relatorios
# de Atendimento e HSM para se autenticar sozinho via POST /rest/v2/authuser
# (ver matrixAuth.js) e obter/renovar o bearer token (~1h de validade)
# automaticamente - sem precisar de sessao de browser nem de renovacao
# manual. Segredo, nao commitar com valor real.
MATRIX_API_LOGIN=
MATRIX_API_SENHA=

# Cache incremental dos relatorios de HSM/Atendimento num MariaDB
# compartilhado (schema API_WebDeveloper, mesmo servidor de outros projetos
# internos - ver reportsDb.js). Sem essas variaveis definidas, o cache fica
# desligado e cada geracao de relatorio busca tudo direto da API (mesmo
# comportamento de antes) - nao e uma dependencia obrigatoria. Segredo, nao
# commitar com valor real. Valores com caracteres especiais (#, espaco, etc)
# precisam de aspas no .env, ex.: MASSIVES_DB_PASSWORD="minha#senha".
MASSIVES_DB_HOST=
MASSIVES_DB_PORT=3306
MASSIVES_DB_USER=
MASSIVES_DB_PASSWORD=
MASSIVES_DB_NAME=
